@model IEnumerable<Employee>

@{
    ViewBag.Title = "Employee List";
}
<div class="card-deck">
    @foreach (var employee in Model)
    {
        <div class="card m-3 ">

            <div class=" card-header ">
                <h3>@employee.Name</h3>
            </div>

            <img class="card-img-top" src="~/images/noimage.jpg" 
                 asp-append-version="true"/>

            <div class=" card-footer text-center">

                <a asp-controller="home" asp-action="details" asp-route-id="@employee.Id" 
    `               class="btn btn-primary m-1">View</a>
                <a href="#" class=" btn btn-primary m-1">Edit</a>
                <a href="#" class=" btn btn-danger m-1">Delete</a>
            
            </div>
        </div>
    }
</div>

<!--image tag helpers help in cache busting-->

<!-- tag helpers are better , coz if a generuc url is changed modified tag helper works fine
    coz it innjects its values in startup.cs code's or Routes as per need -->



<!--//three types of anchor href elements working
    <a href="  atUrl.Action("details", "home", new { id = employee.Id })" class=" btn btn-primary">Show</a>
    atHtml.ActionLink("View","details", "home",new { id = employee.Id})
    <a href="/home/details/atemployee.Id" class=" btn btn-primary">View</a>
    -->
